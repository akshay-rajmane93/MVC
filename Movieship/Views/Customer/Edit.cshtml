@model Movieship.Models.Customer
@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<ul>
    <li>Membership Type: @Model.MemberShip.MenberShipType</li>
    @if (Model.Birthdate.HasValue)
    {
        <li>Birthdate: @Model.Birthdate.Value.ToShortDateString()</li>
    }
</ul>